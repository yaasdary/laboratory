#include <iostream>
#include <cmath>
using namespace std;

int main() {
setlocale(LC_ALL, "Russian");
	int a,b;
	cout << "Количество байт - ";
	cin >> a;
	b=(a/1024);
	if (b % 1024 != 0 || (b>=0 && a!=0)) {
		b++;
	}
	cout << "Количество полных кб - " << b;
	return 0;
	
}
