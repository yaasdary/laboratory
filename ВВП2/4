#include <iostream>
using namespace std;

int main()
{
	double x1, y1, x2, y2, a,b,P,S;
  cout << "(x1;y1), (x2;y2): ";
	cin >> x1 >> y1 >> x2 >> y2;
	a = abs(x1 - x2);
	b = abs(y1 - y2);
	P = 2 * (a + b);
	S = a * b;
	cout << "P=" << P << " " << "S=" << S;
	return 0;
}
